import React from "react";
import PagePagination from "@/components/Pagination/PagePagination";

type Props = {};

const AllDesign = (props: Props) => {
  return <PagePagination path={"all design"} />;
};

export default AllDesign;
